import "./Header.scss"

const Header = () => {
    return (
        <div className="header">
            <span className="text-shadow">very fast processors</span>
        </div>
    );
};

export default Header;